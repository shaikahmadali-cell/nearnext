# Automatic Git Commit & Push Rule

Whenever any modification, fix, or feature is completed in this workspace:
1. Automatically run `git add .`
2. Automatically commit with a concise, descriptive message: `git commit -m "..."`
3. Automatically push to the remote repository: `git push origin main`
